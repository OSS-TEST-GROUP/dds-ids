module security {

  struct DetectionReport {
    string      client_id;    // Security Client ID (최대 15글자 + '\0' 가정 시)
    string      dds_topic;   // 공격이 탐지된 DDS 토픽 이름
    string      rule;        // 탐지된 규칙
    long long timestamp_ms;     // 탐지 시간 (epoch milliseconds)
  };

};